namespace GridDungeon.Core.Hub
{
    /// <summary>Root hub menu rows that map to environment camera anchors (ADR 033).</summary>
    public enum HubRootMenuSlot
    {
        Guild,
        Navigator,
        Shop,
        Hospital,
        Inn,
        EnterStratum,
    }
}
